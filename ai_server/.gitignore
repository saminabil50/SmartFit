# Processed output images — can be regenerated by the AI server
outputs/

# Python virtual environments
.venv/
.venv-*/
__pycache__/
*.pyc
*.pyo

# rembg model cache (downloaded on first use, ~176 MB)
# Located at ~/.u2net/ — not inside this repo
